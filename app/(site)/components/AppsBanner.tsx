import React from 'react'

type Props = {}

export const AppsBanner = (props: Props) => {
  return (
    <div style={{ backgroundImage: `url(/images/backgroundAppsBanner.png)`, width: 'auto', height: '512px', backgroundSize: 'cover', backgroundPosition: 'center' }}></div>
  )
}
